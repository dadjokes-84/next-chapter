# Supabase Setup for Next-Chapter

## Project Info

- **Project URL:** https://vdvctorylfflommtxpps.supabase.co
- **Shared with:** Morsel (same project)

## Environment Variables

Frontend (`.env.local`) and backend (`.env`) are already configured with your Supabase credentials.

## Running Migrations

### Option 1: Via Supabase Dashboard (Recommended)

1. Go to [Supabase Dashboard](https://supabase.com)
2. Select your project
3. Go to **SQL Editor** → **New Query**
4. Copy the entire contents of `supabase/migrations/001_create_tables.sql`
5. Paste and run the query
6. Tables will be created automatically

### Option 2: Via Supabase CLI

```bash
# Install Supabase CLI if not already installed
npm install -g supabase

# Login
supabase login

# Link to your project
supabase link --project-ref vdvctorylfflommtxpps

# Run migrations
supabase db push
```

## Tables Created

- **users** — Core user data (email, password, profile info, verified status)
- **profiles** — Extended user profile (kids count, interests, looking for)
- **matches** — User matches with status tracking
- **messages** — Messages between matched users
- **swipes** — Like/pass history
- **subscriptions** — Stripe subscription tracking
- **message_limits** — Daily message limit tracking for free tier

## Next Steps

1. ✅ Run the migrations (create tables)
2. ⏭️ Start building auth endpoints (signup, login, JWT)
3. ⏭️ Create profile management endpoints
4. ⏭️ Build matching algorithm
5. ⏭️ Implement messaging system with rate limiting

## Notes

- All timestamps are in UTC
- UUIDs are used for all IDs (generated by Supabase)
- Foreign keys enforce referential integrity
- Indexes are created on frequently-queried columns for performance
